#### 1. WAQTD 
      SELECT *
      FROM EMP WHERE COMM IS NULL AND DEPTNO=20;

#### 2.
     SELECT *
     FROM EMP WHERE MGE IS NULL;

#### 4.
    SELECT * 
    FROM EMP WHERE DEPTNO IN('30','20') AND SAL>1500;

#### 10.
    SELECT *
    FROM EMP WHERE DEPTNO IN('30','20') AND MGE IS NULL;            

#### 13.
     SELECT ENAME
     FROM EMP WHERE ENAME LIKE 'M%' OR ENAME LIKE 'J%' OR ENAME LIKE 'A%';

#### 15.
    SELECT ENAME
    FROM EMP WHERE ENAME NOT LIKE '%ES' OR NOT LIKE '%R';

#### 17.
    SELECT *
    FROM EMP WHERE JOB='SALESMAN' AND ENAME LIKE '%E_' AND SAL BETWEEN 100 AND 999;

    SELECT *
    FROM EMP WHERE JOB='SALESMAN' AND ENAME LIKE '%E_' AND SAL LIKE '___';

#### 21.---
    SELECT ENAME
    FROM EMP WHERE SAL NOT BETWEEN('1999','2999') AND DEPTNO NOT BETWEEN('10','20','30') AND JOB!='SALESMAN';

#### 22.

    SELECT DNAME 
    FROM EMP WHERE DNAME LIKE '%O%';

#### 23.
    SELECT ENAME,JOB
    FROM EMP WHERE EMP WHERE JOB LIKE '%MAN%';

#### 24.
    SELECT ENAME 
    FROM EMP WHERE HIREDATE>'31-DEC-1982' AND HIREDATE<'01-JAN-87' AND JOB='ANALYIST';   

#### 25.            
    SELECT *
    FROM EMP WHERE HIREDATE LIKE '%NOV%' OR HIREDATE LIKE '%DEC';    

#### 26.
     SELECT *
     FROM EMP WHERE COMM>SAL;

#### 27.
    SELECT ENAME,SAL,SAL*12 AS ANNUAL_SAL
    FROM EMP WHERE SAL*12 LIKE '_______'AND SAL*12 LIKE '%210';

#### 28.
    SELECT ENAME 
    FROM EMP WHERE ENAME LIKE 'A%' OR ENAME LIKE 'E%' OR ENAME LIKE 'I%' OR ENAME LIKE 'O%' OR ENAME LIKE 'U%';   

    SELECT ENAME
    FROM EMP       
    WHERE LEFT(ENAME,1) = 'A' 
    OR RIGHT(ENAME,1) IN ('E','I','O','U');

#### 29.
    SELECT EMP.*,SAL,SAL*12*1.10 AS ANNUAL_SALARY_HIKE
    FROM EMP
    WHERE DEPTNO IN(10,20,30)
       AND HIREDATE LIKE '%JAN%' 
       AND MGR IS NOT NULL
       AND JOB IN ('SALESMAN','MANAGER')
       AND ENAME LIKE 'S%H'
       AND SAL BETWEEN 1000 AND 4000;    

#### 30.
    SELECT ENAME
    FROM EMP       
    WHERE LEFT(ENAME,1) = 'B' 
    OR RIGHT(ENAME,1) IN ('C','D','F','J','K','L','M','N','P','Q','R','S','T','V','W','X','Y','Z'); 

## CONCATINATION OPERATOR- 
- It is use it to join two string.
    
        SELECT 'MR' || ENAME FROM EMP;       